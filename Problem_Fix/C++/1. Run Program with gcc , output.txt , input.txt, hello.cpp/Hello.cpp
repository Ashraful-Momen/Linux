#include <bits/stdc++.h>
using namespace std;
int main() {
    int num;
    while (cin >> num) {       // read numbers until EOF
        cout << num << endl;   // print each number
    }
    return 0;
}
