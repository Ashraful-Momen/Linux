
```bash
find / -type d -name "anyFolderName" 2>/dev/null
```

### 🔍 Explanation:

* `find /` → start searching from root
* `-type d` → look only for **directories (folders)**
* `-name "anyFolderName"` → match folder name exactly
* `2>/dev/null` → hide “permission denied” errors

---

### Example (search inside home only — faster):

```bash
find ~/ -type d -name "anyFolderName"
```

### find word "passwd" among all file where folder name is "hello.txt"

solv 1: 
--------
┌──(ashraful㉿kali)-[~/Instasure/Digital Card]
└─$ find / -type f -name "hello.txt" -exec grep -Hn "passwd" {} \; 2>/dev/null
/home/ashraful/Instasure/Digital Card/Test/hello.txt:4:passwd: 112233
  
solv 2:
-------
┌──(ashraful㉿kali)-[~/Instasure/Digital Card]
└─$ grep -R --include="hello.txt" -Hn "passwd" ~/
grep: /home/ashraful/.config/gtk-2.0: Permission denied
/home/ashraful/Instasure/Digital Card/Test/hello.txt:4:passwd: 112233

###here -H show the folderName -n show the lineNumber . 
