Here’ **complete mini cheatsheet for `ag` (Silver Searcher)**
to **find the word `passwd` among all files** (and variations for speed & control).

---

## 🔹 1. Basic full-system search

```bash
ag passwd /
```

Searches every readable file under `/` for the word **passwd**.
⚠️ May still hit “Permission denied” — so better to limit scope or use `sudo`.

---

## 🔹 2. Search inside your home directory (safer & faster)

```bash
ag passwd ~
```

---

## 🔹 3. Case-insensitive search

```bash
ag -i passwd ~
```

---

## 🔹 4. Match **whole word** only (not “password”)

```bash
ag -w passwd ~
```

---

## 🔹 5. Show line numbers (default on)

```bash
ag -n passwd ~
```

---

## 🔹 6. Show filenames only (not the matching lines)

```bash
ag -l passwd ~
```

---

## 🔹 7. Include hidden files / folders

```bash
ag --hidden passwd ~
```

---

## 🔹 8. Exclude certain directories (speed up!)

```bash
ag passwd / --ignore node_modules --ignore .git --ignore venv
```

---

## 🔹 9. Search for **exact text (no regex)** (faster)

```bash
ag -Q passwd ~
```

---

## 🔹 10. Combine multiple useful flags

Best for general fast, clean searching:

```bash
ag -i --hidden --ignore {node_modules,.git,venv,cache} -w 'passwd' /
```

* `-i` → case-insensitive
* `--hidden` → include dotfiles
* `--ignore` → skip heavy folders
* `-w` → match whole word
* `'passwd'` → search term
* `/` → search from root (or `~` for home)

---

## 🔹 11. Show only file paths that contain a match

```bash
ag -l 'passwd' /
```

---

## 🔹 12. Limit search to specific file types

```bash
ag 'passwd' --python
ag 'passwd' --php
ag 'passwd' --html
```

You can check all file type shortcuts:

```bash
ag --list-file-types
```

---

## 🔹 13. Search in specific extensions only

```bash
ag 'passwd' --include '*.conf' /etc
```

---

## ⚙️ Optional install (if not installed):

```bash
sudo apt update && sudo apt install silversearcher-ag -y
```

---

✅ **Best all-in-one command for you (safe, fast, complete):**

```bash
ag -i --hidden --ignore {node_modules,.git,venv,cache} -w 'passwd' ~
```

---

