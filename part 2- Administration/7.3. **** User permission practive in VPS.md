# Ubuntu VPS — `sumon_dev` Developer User Setup & Restricted Project Access

## 1. Objective

VPS server-এ `sumon_dev` নামে একজন আলাদা developer user তৈরি করতে হবে।

Developer-এর জন্য:

* আলাদা Linux user থাকবে
* Proper home directory থাকবে
* VS Code Remote SSH ব্যবহার করতে পারবে
* Next.js project install/setup করতে পারবে
* Project server-এ upload করতে পারবে
* Project configure করতে পারবে
* Existing Node.js / npm ব্যবহার করতে পারবে
* Docker ব্যবহার করতে পারবে
* Docker Compose ব্যবহার করতে পারবে
* Git ব্যবহার করতে পারবে
* Claude CLI ব্যবহার করতে পারবে
* Project-এর files/folders create, edit, delete করতে পারবে

কিন্তু security-এর জন্য `ai_root`-এর অন্য files/projects access করতে পারবে না।

### Target Project

```text
/home/ai_root/Project/Safty_Automation_Website
```

---

# 2. Server Environment

### Operating System

```text
Ubuntu 22.04 LTS
```

### Administrator

```text
ai_root
```

### Developer

```text
sumon_dev
```

### Developer Home

```text
/home/sumon_dev
```

### Target Project

```text
/home/ai_root/Project/Safty_Automation_Website
```

### Existing Software

```text
Docker
Docker Compose
Node.js
npm
Git
```

---

# 3. Access Control Requirement

`sumon_dev`-কে পুরো:

```text
/home/ai_root
```

এর access দেওয়া হবে না।

এছাড়া:

```text
/home/ai_root/Project
```

এরও সাধারণ browse/list access থাকবে না।

শুধুমাত্র:

```text
/home/ai_root/Project/Safty_Automation_Website
```

এর উপর full development access থাকবে।

### Permission Model

```text
/home/ai_root
        │
        └── sumon_dev → Traverse only
                │
                ↓
/home/ai_root/Project
        │
        └── sumon_dev → Traverse only
                │
                ↓
/home/ai_root/Project/Safty_Automation_Website
        │
        └── sumon_dev → FULL RWX
```

---

# 4. Create Developer User With Home Directory

এবার home directory সহ user তৈরি করতে হবে।

```bash
sudo useradd -m -s /bin/bash sumon_dev
```

এখানে:

```text
-m
```

মানে automatically:

```text
/home/sumon_dev
```

create করবে।

এবং:

```text
-s /bin/bash
```

মানে Bash shell ব্যবহার করবে।

### Verify

```bash
getent passwd sumon_dev
```

Expected:

```text
sumon_dev:x:1001:1001::/home/sumon_dev:/bin/bash
```

Home directory check:

```bash
ls -ld /home/sumon_dev
```

Expected:

```text
drwx------ ... sumon_dev sumon_dev /home/sumon_dev
```

---

# 5. Set Developer Password

```bash
sudo passwd sumon_dev
```

Secure password set করতে হবে।

---

# 6. Verify User Identity

```bash
id sumon_dev
```

Example:

```text
uid=1001(sumon_dev) gid=1001(sumon_dev) groups=1001(sumon_dev)
```

---

# 7. Developer Home Directory Security

Home directory শুধুমাত্র developer-এর জন্য রাখাই ভালো।

```bash
sudo chmod 700 /home/sumon_dev
sudo chown sumon_dev:sumon_dev /home/sumon_dev
```

Verify:

```bash
ls -ld /home/sumon_dev
```

Expected permission:

```text
drwx------ sumon_dev sumon_dev /home/sumon_dev
```

এখানে developer-এর:

```text
.ssh
.config
.nvm
.claude
.vscode-server
npm configuration
Git configuration
```

থাকতে পারবে।

---

# 8. Give Parent Directory Traverse Permission

Target project nested path:

```text
/home/ai_root/Project/Safty_Automation_Website
```

এই path-এ পৌঁছানোর জন্য parent directories-এ শুধু traverse permission প্রয়োজন।

### `/home/ai_root`

```bash
sudo setfacl -m u:sumon_dev:x /home/ai_root
```

### `/home/ai_root/Project`

```bash
sudo setfacl -m u:sumon_dev:x /home/ai_root/Project
```

এতে `sumon_dev` parent directories traverse করতে পারবে।

কিন্তু অন্য files/folders browse করার জন্য প্রয়োজনীয় read permission থাকবে না।

---

# 9. Give Full Access to Project

Target:

```text
/home/ai_root/Project/Safty_Automation_Website
```

Full access:

```bash
sudo setfacl -m u:sumon_dev:rwx /home/ai_root/Project/Safty_Automation_Website
```

এর ফলে developer project-এর মধ্যে:

```text
Create files
Edit files
Delete files
Create directories
Delete directories
Run commands
Install npm packages
Build project
Configure project
```

করতে পারবে।

---

# 10. Give Access to Existing Project Files

Project-এর ভিতরে আগে থেকে থাকা files/folders-এর জন্য recursive ACL:

```bash
sudo setfacl -R -m u:sumon_dev:rwX /home/ai_root/Project/Safty_Automation_Website
```

এখানে:

```text
-R
```

মানে recursive।

```text
rwX
```

মানে:

* Files → read + write
* Directories → read + write + traverse
* Existing executable files → execute permission preserve/allow

---

# 11. Default ACL for Future Files

এটি খুব গুরুত্বপূর্ণ।

Developer ভবিষ্যতে project-এর ভিতরে নতুন file/folder তৈরি করলে ACL automatically inherit করার জন্য:

```bash
sudo setfacl -d -m u:sumon_dev:rwx /home/ai_root/Project/Safty_Automation_Website
```

Existing subdirectories-এর জন্যও default ACL set:

```bash
sudo find /home/ai_root/Project/Safty_Automation_Website -type d \
  -exec setfacl -m d:u:sumon_dev:rwx {} \;
```

এতে future-created files/folders-এর উপর developer-এর access বজায় থাকবে।

---

# 12. Verify ACL

### `/home/ai_root`

```bash
getfacl /home/ai_root
```

Expected:

```text
user:sumon_dev:--x
```

### `/home/ai_root/Project`

```bash
getfacl /home/ai_root/Project
```

Expected:

```text
user:sumon_dev:--x
```

### Target Project

```bash
getfacl /home/ai_root/Project/Safty_Automation_Website
```

Expected:

```text
user:sumon_dev:rwx
```

এবং:

```text
default:user:sumon_dev:rwx
```

---

# 13. Test Developer Login

Admin থেকে:

```bash
sudo -iu sumon_dev
```

Check:

```bash
whoami
```

Expected:

```text
sumon_dev
```

Home check:

```bash
echo $HOME
```

Expected:

```text
/home/sumon_dev
```

বর্তমানে developer-এর proper home environment থাকবে।

---

# 14. Test Project Access

Developer session থেকে:

```bash
cd /home/ai_root/Project/Safty_Automation_Website
```

Check:

```bash
pwd
```

Expected:

```text
/home/ai_root/Project/Safty_Automation_Website
```

---

# 15. Test File Create/Edit/Delete

Create:

```bash
touch test_sumon.txt
```

Write:

```bash
echo "sumon developer test" > test_sumon.txt
```

Read:

```bash
cat test_sumon.txt
```

Delete:

```bash
rm test_sumon.txt
```

সব successfully কাজ করলে project permission ঠিক আছে।

---

# 16. Test Restricted Access

Developer হিসেবে:

```bash
cd /home/ai_root
```

এখানে traverse করা সম্ভব হবে।

কিন্তু:

```bash
ls /home/ai_root
```

করলে সাধারণভাবে `ai_root`-এর অন্যান্য files/directories list করার permission থাকবে না।

Developer-এর intended access:

```text
/home/ai_root
        → Traverse only

/home/ai_root/Project
        → Traverse only

/home/ai_root/Project/Safty_Automation_Website
        → Full RWX
```

---

# 17. Node.js / npm Access

Server-এ Node.js এবং npm আগে থেকেই installed থাকলে developer user দিয়ে check:

```bash
node -v
npm -v
which node
which npm
```

যদি system-wide installation হয়:

```text
/usr/bin/node
/usr/bin/npm
```

তাহলে `sumon_dev` সরাসরি ব্যবহার করতে পারবে।

---

# 18. NVM — Recommended Setup

যদি Node.js বর্তমানে `ai_root`-এর NVM-এর মধ্যে থাকে:

```text
/home/ai_root/.nvm/...
```

তাহলে `sumon_dev`-কে সেটা share করানো উচিত নয়।

Best practice:

```text
ai_root
└── own NVM

sumon_dev
└── own NVM
```

`sumon_dev`-এর home directory থাকার কারণে নিজের NVM install করা এখন সহজ।

### Install NVM

Developer হিসেবে login করে:

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
```

তারপর:

```bash
source ~/.bashrc
```

NVM verify:

```bash
nvm --version
```

Node 22 install:

```bash
nvm install 22
```

Default Node:

```bash
nvm alias default 22
```

Verify:

```bash
node -v
npm -v
```

এতে `sumon_dev`-এর নিজের Node environment থাকবে।

---

# 19. Claude CLI

`sumon_dev` যদি Claude CLI ব্যবহার করে, নিজের home directory থাকার কারণে আলাদা environment রাখা যাবে।

Recommended:

```text
sumon_dev
├── own NVM
├── own Node.js
├── own npm
├── own Claude CLI
└── own Claude authentication
```

Claude CLI install:

```bash
npm install -g @anthropic-ai/claude-code
```

Check:

```bash
which claude
```

Expected path:

```text
/home/sumon_dev/.nvm/versions/node/.../bin/claude
```

Version:

```bash
claude --version
```

**Important:** `ai_root`-এর Claude credentials/configuration copy বা share করা উচিত নয়।

`sumon_dev` নিজের Claude authentication ব্যবহার করবে।

---

# 20. Docker Access

Server-এ Docker already installed থাকলে `sumon_dev`-কে Docker group-এ add:

```bash
sudo usermod -aG docker sumon_dev
```

Check:

```bash
groups sumon_dev
```

Expected:

```text
sumon_dev ... docker
```

Group change কার্যকর করার জন্য developer-কে নতুন SSH session করতে হবে।

তারপর:

```bash
docker --version
```

Test:

```bash
docker ps
```

---

# 21. Docker Compose Access

Check:

```bash
docker compose version
```

Expected:

```text
Docker Compose version ...
```

Developer project directory থেকে:

```bash
cd /home/ai_root/Project/Safty_Automation_Website
```

তারপর:

```bash
docker compose up -d
```

Build সহ:

```bash
docker compose up -d --build
```

Stop:

```bash
docker compose down
```

Status:

```bash
docker compose ps
```

---

# 22. Docker Security Warning

`docker` group-এ user add করলে user কার্যত Docker daemon-এর মাধ্যমে highly privileged/root-equivalent operations করতে পারে।

অর্থাৎ:

```bash
sudo usermod -aG docker sumon_dev
```

একটি security-sensitive permission।

তাই এটি trusted developer-এর জন্য ব্যবহার করা উচিত।

তোমার internal development/deployment server-এর জন্য এটি practical।

Strict isolation প্রয়োজন হলে Rootless Docker বা আরও শক্ত container/security boundary বিবেচনা করতে হবে।

---

# 23. Git Access

Developer নিজের home directory-তে Git configuration রাখতে পারবে।

Check:

```bash
git --version
```

Project:

```bash
cd /home/ai_root/Project/Safty_Automation_Website
```

Status:

```bash
git status
```

Pull:

```bash
git pull
```

Branch:

```bash
git branch
```

Add:

```bash
git add .
```

Commit:

```bash
git commit -m "Update project"
```

Push:

```bash
git push
```

Git SSH key প্রয়োজন হলে `sumon_dev`-এর:

```text
/home/sumon_dev/.ssh/
```

ব্যবহার করা যাবে।

---

# 24. VS Code Remote SSH

Proper home directory থাকার কারণে VS Code Remote SSH setup সহজ হবে।

Developer PC-এর VS Code SSH config:

```text
Host AI-Dev-Server
    HostName 202.74.246.122
    User sumon_dev
    Port 22
```

VS Code:

```text
Remote Explorer
        ↓
AI-Dev-Server
        ↓
Connect
```

VS Code Server automatically developer-এর home-এ রাখতে পারবে:

```text
/home/sumon_dev/.vscode-server
```

Project open:

```text
/home/ai_root/Project/Safty_Automation_Website
```

---

# 25. SSH Key

Password-এর পরিবর্তে SSH key authentication ব্যবহার করা recommended।

Developer-এর public key:

```text
/home/sumon_dev/.ssh/authorized_keys
```

Permission:

```bash
sudo chown -R sumon_dev:sumon_dev /home/sumon_dev/.ssh
sudo chmod 700 /home/sumon_dev/.ssh
sudo chmod 600 /home/sumon_dev/.ssh/authorized_keys
```

এতে VS Code Remote SSH এবং normal SSH দুটোই secureভাবে ব্যবহার করা যাবে।

---

# 26. Next.js Project Setup

Developer:

```bash
cd /home/ai_root/Project/Safty_Automation_Website
```

Dependencies:

```bash
npm install
```

Development:

```bash
npm run dev
```

Production build:

```bash
npm run build
```

Production start:

```bash
npm start
```

Docker deployment:

```bash
docker compose up -d --build
```

---

# 27. Project Upload

Developer server-এ project upload করতে পারবে:

```text
VS Code Remote SSH
SCP
SFTP
Git
```

Target:

```text
/home/ai_root/Project/Safty_Automation_Website
```

তারপর project configure করবে।

---

# 28. Environment Configuration

Project-এর `.env` বা environment configuration developer manage করতে পারবে যদি project directory-এর ACL-এর মধ্যে থাকে।

Example:

```text
.env
.env.production
.env.local
```

তবে production secrets-এর ক্ষেত্রে security policy অনুসরণ করতে হবে।

বিশেষ করে:

```text
Database passwords
API keys
Cloud credentials
SSH private keys
Production secrets
```

অপ্রয়োজনে developer-এর access দেওয়া উচিত নয়।

---

# 29. Important — Do NOT Share ai_root Environment

`sumon_dev`-এর জন্য আলাদা:

```text
Node/NVM
Claude
Git SSH keys
.npm
.config
.vscode-server
```

ব্যবহার করা উচিত।

Share করা উচিত নয়:

```text
/home/ai_root/.nvm
/home/ai_root/.claude
/home/ai_root/.ssh
/home/ai_root/.config
```

এর কারণ:

```text
Security
Credential isolation
Environment isolation
Version isolation
```

---

# 30. Complete Setup Commands

Admin user `ai_root` থেকে:

```bash
# 1. Create developer user with home
sudo useradd -m -s /bin/bash sumon_dev

# 2. Set password
sudo passwd sumon_dev

# 3. Secure home directory
sudo chown sumon_dev:sumon_dev /home/sumon_dev
sudo chmod 700 /home/sumon_dev

# 4. Parent directory traversal
sudo setfacl -m u:sumon_dev:x /home/ai_root
sudo setfacl -m u:sumon_dev:x /home/ai_root/Project

# 5. Project full access
sudo setfacl -m u:sumon_dev:rwx \
/home/ai_root/Project/Safty_Automation_Website

# 6. Existing project files/folders
sudo setfacl -R -m u:sumon_dev:rwX \
/home/ai_root/Project/Safty_Automation_Website

# 7. Default ACL
sudo setfacl -d -m u:sumon_dev:rwx \
/home/ai_root/Project/Safty_Automation_Website

# 8. Default ACL for existing subdirectories
sudo find /home/ai_root/Project/Safty_Automation_Website -type d \
  -exec setfacl -m d:u:sumon_dev:rwx {} \;

# 9. Docker access
sudo usermod -aG docker sumon_dev
```

---

# 31. Verification Commands

### User

```bash
id sumon_dev
```

### Home

```bash
ls -ld /home/sumon_dev
```

### Home variable

Developer session:

```bash
echo $HOME
```

Expected:

```text
/home/sumon_dev
```

### Project ACL

```bash
getfacl /home/ai_root/Project/Safty_Automation_Website
```

### Parent ACL

```bash
getfacl /home/ai_root
getfacl /home/ai_root/Project
```

### Docker

```bash
docker --version
docker compose version
docker ps
```

### Node

```bash
node -v
npm -v
```

### Git

```bash
git --version
```

### Claude

```bash
claude --version
```

---

# 32. Developer Final Capability

`sumon_dev` successfully configured হলে developer:

```text
✅ SSH login
✅ VS Code Remote SSH
✅ Own home directory
✅ Own .ssh
✅ Own .config
✅ Own NVM
✅ Own Node.js
✅ Own npm
✅ Own Claude CLI
✅ Own Claude authentication
✅ Git
✅ Docker
✅ Docker Compose
✅ Next.js development
✅ npm install
✅ npm build
✅ npm run dev
✅ npm start
✅ Project upload
✅ Project configuration
✅ Project file create
✅ Project file edit
✅ Project file delete
```

---

# 33. Developer Restricted Access

Developer-এর access সীমাবদ্ধ থাকবে:

```text
/home/ai_root
        → Traverse only

/home/ai_root/Project
        → Traverse only

/home/ai_root/Project/Safty_Automation_Website
        → Full RWX
```

Developer সাধারণভাবে access করতে পারবে না:

```text
❌ /home/ai_root/.ssh
❌ /home/ai_root/.claude
❌ /home/ai_root/.nvm
❌ /home/ai_root/.config
❌ ai_root-এর অন্য project
❌ ai_root-এর personal files
```

---

# 34. Final Architecture

```text
Ubuntu 22.04 LTS
│
├── ai_root
│   │
│   ├── Administrator
│   ├── Docker
│   └── Project
│       │
│       └── Safty_Automation_Website
│                         ↑
│                         │
│                  ACL: sumon_dev:rwx
│
└── sumon_dev
    │
    ├── /home/sumon_dev
    │
    ├── .ssh
    ├── .config
    ├── .vscode-server
    ├── .nvm
    ├── .npm
    ├── .claude
    │
    ├── Node.js
    ├── npm
    ├── Claude CLI
    ├── Git
    ├── Docker
    └── Docker Compose
```

---

# 35. Final Permission Model

```text
                    Ubuntu VPS
                        │
          ┌─────────────┴─────────────┐
          │                           │
       ai_root                    sumon_dev
          │                           │
    Full server/admin          Developer account
          │                           │
          │                    /home/sumon_dev
          │                           │
          │                    Own environment
          │                    Node/NVM/Claude
          │                           │
          └──────────────┬────────────┘
                         │
                         ↓
       /home/ai_root/Project/Safty_Automation_Website
                         │
                         │ ACL
                         ↓
                 sumon_dev: RWX
```

---

# 36. Final Recommendation

এই architecture-টাই তোমার requirement-এর জন্য recommended:

```text
Developer User
    ↓
/home/sumon_dev
    ↓
Own SSH / VS Code / NVM / Node / Claude
    ↓
Docker + Docker Compose
    ↓
ACL-based Project Access
    ↓
/home/ai_root/Project/Safty_Automation_Website
```

এতে `sumon_dev` একজন proper developer হিসেবে server-এ কাজ করতে পারবে, কিন্তু `ai_root`-এর অন্যান্য project এবং personal configuration-এর access পাবে না।

**Security boundary হবে ACL, আর developer environment হবে আলাদা home directory।**
